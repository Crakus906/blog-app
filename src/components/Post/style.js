import styled from 'styled-components';

export const PostWrapper = styled.div`
    display: flex;
    flex-direction: column;
    padding: 1rem;
    .title {
        color: black;
        font-size: 18px;
        margin-top: -1.3rem;
    }
    .body {
        color: black;
    }
`;